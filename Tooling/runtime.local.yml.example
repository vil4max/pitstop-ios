# Local overrides (gitignored). Example:
# simulator:
#   name: iPhone 17
#   os: "26.5"
#   # Reserve a device so another project's tests cannot take it over:
#   #   xcrun simctl create "MyApp iPhone 17" "iPhone 17"   # prints the UDID
#   udid: 00000000-0000-0000-0000-000000000000
# backend:
#   prefer: xcodebuild
